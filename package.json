{
  "name": "trailguide",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @trailguide/editor dev",
    "dev:vanilla": "npx serve examples/vanilla-demo",
    "dev:website": "npx serve website",
    "build": "pnpm -r build",
    "build:core": "pnpm --filter @trailguide/core build",
    "build:runtime": "pnpm --filter @trailguide/runtime build",
    "build:recorder": "pnpm --filter @trailguide/recorder build",
    "build:editor": "pnpm --filter @trailguide/editor build",
    "typecheck": "pnpm -r typecheck",
    "dev:demo": "pnpm --filter @trailguide/editor dev",
    "seed:analytics": "pnpm --filter @trailguide/editor seed:analytics",
    "clean": "pnpm -r exec rm -rf dist node_modules"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "pnpm": {
    "overrides": {
      "@types/react": "18.3.28",
      "@types/react-dom": "18.3.5"
    }
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
